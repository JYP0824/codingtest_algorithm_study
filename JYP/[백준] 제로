# K번 반복할 수를 받음
# 반복하는 동안 입력을 받는다 (받은 입력이 '0'일 경우 pop, 다른 수일 경우 push
# 그 후 list 내의 모든 수를 합한다

num = int(input())
a = []

for i in range(num):
    b = int(input())
    
    if b == 0:
        a.pop()
    else:
        a.append(b)
print(sum(a))
